package com.mahweb.mahyarnfc.omnishare.transport; import com.mahweb.mahyarnfc.omnishare.*; import java.util.*; public interface OmniTransport extends AutoCloseable { TransportKind kind(); boolean isAvailable(); List<Recipient> discoverRecipients(long timeoutMs)throws Exception; TransportCandidate evaluate(Recipient r); DeliveryAck send(Recipient r,TransferEnvelope e)throws Exception; void cancel(String transferId); void close(); }
